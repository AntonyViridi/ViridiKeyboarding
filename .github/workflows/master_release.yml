name: Build ViridiKeyboard

on:
  push:
    branches: [ master ]
    paths:
      - 'config/**'
      - 'boards/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 3️⃣ Install Python dependencies
      - name: Install Python dependencies
        run: pip install --upgrade pip west pyserial pyelftools

      # 4️⃣ Install system dependencies
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build gperf ccache dfu-util \
                                  device-tree-compiler wget xz-utils file git

      # 5️⃣ Clone ZMK repository
      - name: Clone ZMK main
        run: |
          git clone --depth 1 https://github.com/zmkfirmware/zmk-main.git zmk

      # 6️⃣ Update ZMK modules
      - name: Update ZMK modules
        run: |
          cd zmk
          west update || echo "No modules to update"

      # 7️⃣ Install Zephyr SDK v0.16.3
      - name: Install Zephyr SDK
        run: |
          wget https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v0.16.3/zephyr-sdk-0.16.3_linux-x86_64.tar.xz
          tar -xf zephyr-sdk-0.16.3_linux-x86_64.tar.xz
          ./zephyr-sdk-0.16.3/setup.sh -t all -c
          echo "ZEPHYR_SDK_INSTALL_DIR=$PWD/zephyr-sdk-0.16.3" >> $GITHUB_ENV

      # 8️⃣ Export Zephyr environment
      - name: Export Zephyr environment
        run: source zmk/zephyr/zephyr-env.sh

      # 9️⃣ Copy custom shield files
      - name: Copy custom shield
        run: |
          mkdir -p zmk/app/boards/shields/viridikeyboard
          cp -r boards/shields/viridikeyboard/* zmk/app/boards/shields/viridikeyboard/

      # 🔟 Build firmware
      - name: Build firmware
        run: |
          west build -s zmk/app -d build -b nrf52840_m2 -- -DSHIELD=viridikeyboard

      # 1️⃣1️⃣ Upload firmware artifact
      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: build/zephyr/*.uf2
