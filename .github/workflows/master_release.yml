name: Build ViridiKeyboard

on:
  push:
    branches: [ master ]
    paths:
      - 'config/**'
      - 'boards/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false

      # 2️⃣ Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 3️⃣ Install Python dependencies
      - name: Install Python dependencies
        run: pip install --upgrade pip west pyserial pyelftools

      # 4️⃣ Install system dependencies
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build gperf ccache dfu-util \
                                  device-tree-compiler wget xz-utils file unzip

      # 5️⃣ Clone ZMK
      - name: Clone ZMK repository
        run: |
          git clone --branch main https://github.com/zmkfirmware/zmk.git zmk
          cd zmk
          west init -l app
          west update

      # 6️⃣ Install Zephyr SDK
      - name: Install Zephyr SDK
        run: |
          wget https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v0.16.3/zephyr-sdk-0.16.3_linux-x86_64.tar.xz
          tar -xf zephyr-sdk-0.16.3_linux-x86_64.tar.xz
          ./zephyr-sdk-0.16.3/setup.sh -t all -c
          echo "ZEPHYR_SDK_INSTALL_DIR=$PWD/zephyr-sdk-0.16.3" >> $GITHUB_ENV

      # 7️⃣ Export Zephyr environment
      - name: Zephyr environment
        run: |
          cd zmk
          source zephyr/zephyr-env.sh

      # 8️⃣ Copy custom shield
      - name: Copy custom shield
        run: |
          cd zmk/app
          mkdir -p boards/shields/viridikeyboard
          cp -r ../../../boards/shields/viridikeyboard/* boards/shields/viridikeyboard/

      # 9️⃣ Build firmware
      - name: Build firmware
        run: |
          cd zmk/app
          west build -b nrf52840_m2 -d build -DSHIELD=viridikeyboard

      # 🔟 Upload firmware artifact
      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: zmk/app/build/zephyr/*.uf2
